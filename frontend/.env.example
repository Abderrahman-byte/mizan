# Base URL of the Mizan backend API. Only consumed once real API integration lands;
# the app currently runs entirely on the in-memory mock layer (see docs/mock-data.md).
VITE_API_URL=http://localhost:8000/api
