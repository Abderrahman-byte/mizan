# Mizan backend — runtime dependencies (pip, no lockfile by decision).
# Stack is fixed; do not add libraries without confirming (see CLAUDE.md).
fastapi>=0.115
uvicorn[standard]>=0.30
sqlalchemy[asyncio]>=2.0
asyncpg>=0.29
alembic>=1.13
pydantic>=2.7
pydantic-settings>=2.3
loguru>=0.7
